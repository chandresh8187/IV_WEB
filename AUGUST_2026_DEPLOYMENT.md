# August 2026 deployment

1. Set `VITE_API_BASE_URL` to the deployed API URL.
2. Run `npm install`.
3. Run `npm run build`.
4. Deploy the generated `dist` directory.

The browser stores unsent supervisor production entries locally and automatically retries them when the connection returns. Do not clear site data while offline entries are waiting.
